const buffer = Buffer.from("Bernardo")

console.log(buffer)
console.log(buffer.toJSON())

// Um buffer eh uma estrutura de dados que armazena os dados em formato Binario ( em bytes )
// facilia e otimiza a leitura para a maquina para as streams do node